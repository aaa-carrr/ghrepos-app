import Foundation

public struct ReposTableViewCellViewModel {
    let repoName: String
    let repoDescription: String
    let repoAuthor: UserProfileCompactViewModel
    let repoStars: String
    let repoForks: String
}
